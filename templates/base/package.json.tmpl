{
  "name": "{{name}}",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "{{startScript}}",
    "dev": "{{devScript}}"
  },
  "dependencies": {
    "spectrum-ts": "{{spectrumTsVersion}}"
  },
  "devDependencies": {
    "typescript": "^5"{{extraDevDeps}}
  }
}
